# Test requirements.txt file for dependency confusion detection
# Contains legitimate packages mixed with suspicious internal packages

# Standard legitimate packages
requests>=2.28.0
numpy>=1.21.0
pandas>=1.4.0
flask>=2.1.0
django>=4.0.0
fastapi>=0.75.0

# Suspicious internal/private packages (should be flagged)
internal-auth-service==2.1.0     # Internal authentication service
company-database-connector>=1.5.0   # Private database connector
private-logging-utils==0.8.5        # Internal logging utilities  
enterprise-config-manager>=2.0.0    # Enterprise configuration tool
org-secret-vault>=1.2.1            # Organization secret management
custom-security-lib==3.1.4         # Custom security library
internal-api-client>=0.9.2         # Internal API client
company-ml-toolkit==1.0.8          # Private ML toolkit
private-metrics-collector>=2.3.0    # Internal metrics collection
enterprise-deployment-tools==1.4.2  # Enterprise deployment utilities

# More legitimate packages
boto3>=1.24.0
sqlalchemy>=1.4.0
celery>=5.2.0
redis>=4.3.0

# Additional suspicious packages with different version formats
internal-shared-models~=1.2.3       # Shared internal models (~= operator)
company-auth-middleware>1.0.0       # Auth middleware (> operator)
private-data-processor<2.0.0        # Data processor (< operator)
org-notification-service!=1.5.0     # Notification service (!= operator)

# Git-based suspicious packages (should trigger GitHub hijacking checks)
git+https://github.com/abandoned-company/internal-tools.git@main
git+https://github.com/missing-org/private-utils.git#egg=private-utils
git+ssh://git@github.com/nonexistent-corp/secret-lib.git

# Packages with file:// URLs (local development packages)
file:///opt/internal-packages/company-core
-e file:///home/dev/private-projects/internal-sdk

# Test comments and edge cases
# This is a comment - should be ignored
    # Indented comment

# Package with inline comments
pytest>=7.0.0  # Testing framework
company-test-utils>=1.0.0  # Internal testing utilities (should be flagged)

# Empty lines should be handled gracefully


# Packages with complex version specifiers
internal-complex-lib>=1.0.0,<2.0.0,!=1.3.0  # Complex version spec
company-multi-env>=1.0.0; python_version>="3.8"  # Environment markers

# End of requirements.txt test file
